<%= form_with(model: resort, local: true) do |form| %>
  <% if resort.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resort.errors.count, "error") %> prohibited this resort from being saved:</h2>

      <ul>
        <% resort.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :resort_name %>
    <%= form.text_field :resort_name %>
  </div>

  <div class="field">
    <%= form.label :resort_url %>
    <%= form.text_field :resort_url %>
  </div>

  <div class="field">
    <%= form.label :recent_time %>
    <%= form.text_field :recent_time %>
  </div>

  <div class="field">
    <%= form.label :recent_snowfall %>
    <%= form.number_field :recent_snowfall %>
  </div>

  <div class="field">
    <%= form.label :next_snowfall %>
    <%= form.number_field :next_snowfall %>
  </div>

  <div class="field">
    <%= form.label :next5_snowfall %>
    <%= form.number_field :next5_snowfall %>
  </div>

  <div class="field">
    <%= form.label :state_id %>
    <%= form.number_field :state_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
